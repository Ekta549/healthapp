import 'package:flutter/material.dart';

class Emotic extends StatelessWidget {
  const Emotic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
